# secrets
.env
*.env
!.env.example
!.env.local.example
.env.local
.env*.local

# python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.ruff_cache/
.venv/
venv/

# derived data
data/chroma/

# corpus documents: re-fetchable from data/corpus/manifest.csv via
# ./scripts/fetch_corpus.sh, so the manifest is tracked and the bytes are not
data/corpus/files/

# node / next
node_modules/
.next/
out/

# os
.DS_Store

# installers / binaries (not source, re-downloadable)
*.exe

# vendored Tesseract build: extracted from the installer, ~100MB of binaries.
# Recreate with: 7z x tesseract-ocr-w64-setup-*.exe -o vendor/tesseract
vendor/

# D-28 vision model comparison: fetched figures + raw model outputs (regenerate
# with scripts/vision_model_compare.py)
scripts/vision_cmp_out/
