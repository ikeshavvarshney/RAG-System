# PUBLIC VALUE. NEXT_PUBLIC_* variables are inlined into the client bundle at build time and are readable by anyone who loads the page. Never put a secret, API key or access token in this file or in any NEXT_PUBLIC_* variable.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000