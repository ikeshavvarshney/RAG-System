# Comma-separated API key pools (round-robin rotation across free-tier keys)
GEMINI_API_KEYS=
TAVILY_API_KEYS=

# Vector store path (derived data, gitignored)
CHROMA_PATH=./data/chroma

# Frontend origin for CORS (never use "*")
FRONTEND_ORIGIN=http://localhost:3000

# Chunking contract (tokens)
CHUNK_MIN_TOKENS=200
CHUNK_MAX_TOKENS=800
CHUNK_TARGET_TOKENS=500

# --- OCR ---
# Which OCR engine to attempt first. Each falls through to the next when it is
# unavailable, so this chooses what is tried, not what is required.
#   paddle-structure  PP-StructureV3: layout + table structure as markdown.
#                     ~75s/page on CPU. The RQ2 baseline, not a production
#                     setting. Needs the [ocr] extra: pip install -e ".[ocr]"
#   paddle            plain PaddleOCR recognition, ~3s/page (default)
#   tesseract         skip paddle entirely
# OCR_ENGINE=paddle
# OCR_LANG=en
# Detections below this score are dropped; paddle boxes page rules as text.
# OCR_MIN_CONFIDENCE=0.5
# Rotated-line classification. Costs a model per image; corpus is upright.
# OCR_TEXTLINE_ORIENTATION=false

# Path to the Tesseract binary, not the directory that holds it. A relative
# path is resolved against the repo root, not the working directory, so the
# same value works under uvicorn and under pytest. Defaults to the vendored
# copy; override for a system-wide install (e.g.
# C:\Program Files\Tesseract-OCR\tesseract.exe).
# TESSERACT_CMD=vendor/tesseract/tesseract.exe

# --- Vision & Embedding  ---
EMBEDDING_MODEL=gemini-embedding-001
VISION_MODEL=gemini-3.6-flash
MAX_VISION_PAGES=80
EMBEDDING_CACHE_DIR=./data/cache/embeddings
VISION_CACHE_DIR=./data/cache/vision
